__pycache__/
*.py[cod]
*.so

.Python
.venv/
venv/
env/

.pytest_cache/
.mypy_cache/
.ruff_cache/

.DS_Store
Thumbs.db

# Large local artifacts: keep these outside git and record paths in manifests.
outputs/
opencood/logs/
data/
dataset/

# Local checkpoint copies used to make this workspace runnable without external
# checkpoint directories. Keep weights out of git; track small config metadata.
!opencood/detection/checkpoints/
!opencood/detection/checkpoints/**
opencood/detection/checkpoints/**/*.pth
opencood/detection/checkpoints/**/*.pt
!opencood/fusion/checkpoints/
!opencood/fusion/checkpoints/**
opencood/fusion/checkpoints/**/*.pth
opencood/fusion/checkpoints/**/*.pt

# Experiment/runtime noise.
*.log
*.jsonl
events.out.tfevents.*
wandb/
