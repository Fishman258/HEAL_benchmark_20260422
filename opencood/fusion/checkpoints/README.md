Fusion checkpoints copied into this workspace for local benchmark runs.

Current local checkpoint directories:

- `opv2v_camera_v2xvit_full_prope`: OPV2V camera V2X-ViT config and `net_epoch_bestval_at21.pth`.
- `pubmap_full_heal_pointpillar_2026_05_08_16_08_40`: PubMap PointPillar config and `net_epoch_bestval_at51.pth`.

The `.pth` files are intentionally ignored by git. See `benchmarks/manifests/paths.yaml` for source paths and roles.
